#include "sysdep.h"
#include "opcode/myemulator2.h"

/* The MyEmulator2 decoder is deliberately table-free: the encoding manifest
   is regular enough that the disassembler decodes fields directly.  This
   translation unit exists for the normal opcodes library target structure. */
